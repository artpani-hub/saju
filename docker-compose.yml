version: '3.8'

services:
  hyeandang:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hyeandang-saju
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: always
